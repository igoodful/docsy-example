---
title: 3. 函数
description: function
date: 2023-10-30
weight: 30000


viz: true
---

{{< alert >}}


{{< /alert >}}

## 定义

```viz-dot
digraph "func" {
        rankdir=TB;
        edge [
                fontsize="12"
                ];
        node [
                shape = octagon;
                fontsize = "12"
                ];
        "函数" -> "定义";
        "定义" -> "组成";
        "组成" -> "返回值类型";
        "组成" -> "函数名称";
        "组成" -> "形参";
        "组成" -> "函数体";
        "定义" -> "程序代码文件";
        "定义" -> "只有一份";
        "定义" -> "不能嵌套定义";
        "程序代码文件" -> "只编译一次";
        "程序代码文件" -> "链接";
        "函数" -> "声明";
        "声明" -> "1. 头文件，适合跨文件\n 2. cc文件，适合本地文件" ;
        "函数" -> "调用";
}
```


## 声明
```viz-dot
digraph "func" {
        rankdir=TB;
        edge [
                fontsize="12"
                ];
        node [
                shape = octagon;
                fontsize = "12"
                ];

        "函数" -> "声明";
        "声明" -> "     1. 返回值类型\n2. 函数名称\n3. 参数列表" ;
        "声明" -> "1. 一般放在头文件，适合跨文件\n2. 源代码中声明，适合本地文件" ;
        "声明" -> "可以有多份" ;
}
```


## 调用


## 形参

```viz-dot
digraph "形参" {
        rankdir=TB;
        edge [
                fontsize="12"
                ];
        node [
                shape = octagon;
                fontsize = "12"
                ];
        "形参" -> "参数类型";
        "参数类型" -> "基本类型";
        "参数类型" -> "结构体";
        "参数类型" -> "函数指针";
        "参数类型" -> "数组";
        "参数类型" -> "指针";
        "参数类型" -> "引用";
        "形参" -> "默认参数值";
        "结构体" -> "结构体名称";
        "结构体" -> "结构体指针" [label="是否需要const？"];
        "结构体" -> "结构体引用";
}
```


## 返回值





```viz-dot
digraph "返回值类型" {
        rankdir=TB;
        edge [
                fontsize="12"
                ];
        node [
                shape = octagon;
                fontsize = "12"
                ];
        "返回值类型" -> "不能为数组";
        "返回值类型" -> "返回局部静态类型";
        "返回值类型" -> "返回局部基本类型值";
        "返回值类型" -> "不能返回局部非静态指针和引用";
}
```
































